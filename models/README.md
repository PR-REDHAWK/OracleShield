# Trained artifacts

Place the following locally (they are intentionally excluded from Git):

- `model_classifier.joblib`
- `scaler.joblib`
- `feature_columns.joblib`
- `world_model.pt` (generated by `train_world_model.py`)

These files are excluded by `.gitignore` because trained binaries and
datasets can be large and should not be committed accidentally.
